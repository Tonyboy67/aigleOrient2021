{% extends 'base.html.twig' %}

{% block title %}新用户 - Nouvel utilisateur
{% endblock %}

{% block body %}

	<div class="container pt-5" style="width: 60%">
		<h2>新用户 - Nouvel utilisateur</h2>
		{{ form_start(registrationForm) }}
		{# {{ form_row(registrationForm.email) }}
						{{ form_row(registrationForm.plainPassword, {
							label: '密码 - Mot de passe'
						}) }}
						{{ form_row(registrationForm.agreeTerms) }} #}

		{{form_widget(registrationForm)}}
		<button type="submit" class="btn-primary">保存 - Enregistrer</button>
		{{ form_end(registrationForm) }}
	</div>
	<style>
		label {
			font-weight: bolder;
			margin-bottom: 5px;
		}
		input,
		textarea {
			font-weight: bolder;
			margin-bottom: 15px;
		}
	</style>
{% endblock %}
